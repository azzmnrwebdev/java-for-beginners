public class ForEach {
    public static void main(String[] args) {
        String[] names = {
                "Azzam", "Nur", "Alwi", "Mansyur",
                "Lovina", "Aulia"
        };

        for (var name: names) {
            System.out.println(name);
        }
    }
}
